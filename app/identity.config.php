<?php
// $directory_config  = opendir("app/config");
// while (false !== ($view_directory = readdir($directory_config))) {
// 	if(preg_match("/.php/", $view_directory)){
// 		include "../modules/".$get_name."/controller/config.php";
// 		foreach($install['directory'] as $directory){
// 			if(!empty($directory)){
// 				if( is_dir($directory) === false ){
// 					mkdir($directory, 0777);
// 				}
// 			}
// 		}
// 	}
// }
?>
